CREATE DATABASE auth_db;
USE auth_db;

CREATE TABLE users(
    id INT AUTO_INCREMENT PRIMARY key,
    emil VARCHAR(255) UNIQUE NOT NULL,
    password VARCHAR(255) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);